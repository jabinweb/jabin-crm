import UserProfileSettings from '@/components/settings/user-profile/user-profile-settings'

/** Personal CRM profile (UserProfile): AI keys, SMTP/IMAP, invoicing defaults — not the workspace `Company` record. */
export default function PersonalCrmSettingsPage() {
  return <UserProfileSettings />
}
