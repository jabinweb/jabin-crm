-- No-op migration.
--
-- This migration was generated during a prior schema sync attempt and included
-- unsafe constraint renames that fail on shadow databases. It is intentionally
-- left as a no-op to preserve migration history ordering.
SELECT 1;
